node_modules/
dist/
.DS_Store

# Only glTF is actually loaded by the game; the other export formats are
# ~80MB of unused duplicates kept on disk for reference but not tracked.
public/assets/**/Blends/
public/assets/**/FBX/
public/assets/**/OBJ/

# Raw Kenney onscreen-controls source dump (8 styles x sizes, ~7MB); only
# the 4 PNGs actually used are copied into public/assets/mobile-controls/.
/mobile-controls-1 (3)/
